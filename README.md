# BEER Küchenmanufaktur

Next.js frontend for the BEER website. Content comes from Strapi.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). Set `STRAPI_URL` (and `NEXT_PUBLIC_STRAPI_URL` if needed) to the CMS origin.
